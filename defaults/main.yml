---
ntp_package: chrony
ntp_servers:
  - 0.pool.ntp.org iburst
  - 1.pool.ntp.org iburst
  - 2.pool.ntp.org iburst
  - 3.pool.ntp.org iburst
ntp_service: chrony
ntp_driftfile: /var/lib/chrony/drift
ntp_logdir: /var/log/chrony
custom_utc: "Europe/Paris"
